extends layout

block content
  // /.blog-main
  #posts
      - var posts = getPosts(0,5)
      - var page  = 1
      - each post in posts
          include partials/post
          .text-center
            include includes/pagination
